---
all:
 vars:

  # GKE cluster variables
  gcp_project:  ascn-grupo39               # ID of GCP Project
  gcp_auth_kind: serviceaccount            # Do not change
  gcp_cred_file: ascn-grupo39-377181257e7a.json    # Path to service account keys (json file downloaded from GCP)

  gcp_zone: us-central1-a
  gcp_image_type: ubuntu_containerd
  gcp_machine_type: e2-medium          # Can be changed if necessary
  gcp_disk_size_gb: 100                # Can be changed if necessary
  gcp_initial_node_count: 4            # Number of nodes to create. Can be changed if necessary


  # APP variables
  app_ip: "35.223.110.100"
  app_port: 80        

  dashboard_config_file: "dashboards.json"
  dashboard_name: "LaravelIO - Dashboard"
  # Additional variables
  # ...gke_hosts
